{% block MainNav %}
<header id="header">
    <div class="color-line">
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    {#<a class="navbar-brand" href="{{ path('emotion_homepage') }}">Home</a>#}
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li class="active"><a href="{{ path('emotion_homepage') }}">Accueil <span class="sr-only">(current)</span></a></li>
                        <li><a href="{{ path('emotion_products') }}">Rechercher</a></li>
                        <li class="dropdown" style = "display:none;">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rechercher par type de véhicule <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                {% for c in category %}
                                    <li><a href="{{ path('emotion_categories', {'id': c.id}) }}">{{ c.category }}</a></li>
                                {% endfor %}
                            </ul>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rechercher par marques <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                {% for b in brand %}
                                    <li><a href="{{ path('emotion_brands', {'id': b.id}) }}">{{ b.brand }}</a></li>
                                {% endfor %}
                            </ul>
                        </li>
                        <li><a href="{{ path('emotion_contact') }}">Contactez-nous</a><li>

                         {% if is_granted('ROLE_USER') %}
                            <li><a href="{{ path('emotion_admin') }}">admin</a><li>
                         {% endif %}

                    </ul>
                    {#<form class="navbar-form navbar-left">#}
                        {#<div class="form-group">#}
                            {#<input type="text" class="form-control" placeholder="Search">#}
                        {#</div>#}
                        {#<button type="submit" class="btn btn-default">Submit</button>#}
                    {#</form>#}
                    <ul class="nav navbar-nav navbar-right">
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('emotion_admin') }}">Admin</a><li>
                        {% endif %}
                        {% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('emotion_profil', {'id': id.id}) }}">Profil</a></li>
                        {% else %}
                            <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                        {% endif %}
                        {% if is_granted('ROLE_USER') %}
                            <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
                        {% else %}
                            <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                        {% endif %}
                    </ul>
                </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
        </nav>
    </div>
</header>
{% endblock %}
{% block JS %}
{% endblock %}